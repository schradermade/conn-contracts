name: Schema CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JSON parses
        run: |
          for f in schemas/*.json; do
            jq empty "$f"
          done
